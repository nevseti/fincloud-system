# Улучшения раздела 2.2 - Сетевое взаимодействие

## 📍 Где и что исправить:

---

### 1. РАСШИРИТЬ АБЗАЦ ПРО СЕТЬ (строка 135)

**БЫЛО:**
```
Контейнеры сервисов объединены общей внутренней сетью Docker Swarm, обеспечивающей безопасное межсервисное взаимодействие в пределах одного стека. Доступ снаружи осуществляется через открытые порты фронтенд-сервиса и соответствующих backend-сервисов, к которым frontend обращается напрямую. Frontend не использует отдельного API-шлюза: JavaScript-код выполняет запросы непосредственно к auth-service, finance-service и report-service через их публичные HTTP endpoints. Такой подход упрощает архитектуру, сохраняя при этом полную изолированность инфраструктуры на уровне контейнерной сети.
```

**СТАЛО:**
```
Контейнеры сервисов объединены общей внутренней overlay-сетью Docker Swarm с именем `fincloud-network`, обеспечивающей безопасное межсервисное взаимодействие в пределах одного стека. Overlay-сеть представляет собой виртуальную сеть, создаваемую поверх физической сети хоста, которая позволяет контейнерам общаться друг с другом как будто они находятся в одной локальной сети. Docker Swarm автоматически создаёт DNS-записи для каждого сервиса, позволяя обращаться к ним по имени: например, сервис `auth-service` доступен по адресу `auth-service:8000` внутри сети, а `postgres` – по адресу `postgres:5432`. Такая DNS-резолюция упрощает конфигурацию и обеспечивает автоматическую маршрутизацию трафика между контейнерами.

Доступ снаружи осуществляется через открытые порты фронтенд-сервиса и соответствующих backend-сервисов, к которым frontend обращается напрямую. Frontend не использует отдельного API-шлюза: JavaScript-код выполняет запросы непосредственно к auth-service, finance-service и report-service через их публичные HTTP endpoints (например, `http://localhost:8000/login` для аутентификации и `http://localhost:8001/balance` для получения баланса). Backend-сервисы взаимодействуют друг с другом через внутренние адреса overlay-сети: report-service обращается к finance-service по адресу `http://finance-service:8001`, что обеспечивает более быструю и безопасную коммуникацию без необходимости прохождения через внешние порты.

Для обеспечения доступа к сервисам с хост-машины настроен Port Forwarding VirtualBox, который перенаправляет трафик с портов хоста на соответствующие порты внутри виртуальной машины: порт 8080 хоста → порт 80 контейнера frontend, порт 8000 хоста → порт 8000 контейнера auth-service, порт 8001 хоста → порт 8001 контейнера finance-service, порт 8002 хоста → порт 8002 контейнера report-service. Такая конфигурация обеспечивает доступ к системе через `http://localhost:8080` с хост-машины, при этом все межсервисное взаимодействие происходит внутри изолированной overlay-сети.

Безопасность сетевого взаимодействия обеспечивается на нескольких уровнях: PostgreSQL доступен только внутри overlay-сети и недоступен напрямую извне, что исключает возможность несанкционированного доступа к базе данных; все backend-сервисы настроены для обработки cross-origin-запросов (CORS) от фронтенда; все защищённые endpoints требуют наличия валидного JWT-токена в заголовке Authorization; report-service не имеет прямого доступа к PostgreSQL и получает данные только через API finance-service, что соответствует принципам микросервисной архитектуры. Такой подход упрощает архитектуру, сохраняя при этом полную изолированность инфраструктуры на уровне контейнерной сети.
```

---

### 2. ДОБАВИТЬ ТАБЛИЦУ ПОСЛЕ СТРОКИ 135 (опционально, но рекомендуется)

**ДОБАВИТЬ ПОСЛЕ РАСШИРЕННОГО АБЗАЦА ПРО СЕТЬ:**

```
Таблица 4 – Сетевая конфигурация сервисов системы FinCloud

| Сервис | Внутренний адрес в overlay-сети | Порт | Внешний порт (Port Forwarding) | Доступность |
|--------|----------------------------------|------|--------------------------------|-------------|
| Frontend | frontend:80 | 80 | 8080 | Публичный (через Port Forwarding) |
| Auth-service | auth-service:8000 | 8000 | 8000 | Публичный (через Port Forwarding) |
| Finance-service | finance-service:8001 | 8001 | 8001 | Публичный (через Port Forwarding) |
| Report-service | report-service:8002 | 8002 | 8002 | Публичный (через Port Forwarding) |
| PostgreSQL | postgres:5432 | 5432 | - | Только внутри overlay-сети |
```

---

### 3. ДОПОЛНИТЕЛЬНО: РАСШИРИТЬ АБЗАЦ ПРО VOLUMES (строка 136) - опционально

**БЫЛО:**
```
Для хранения данных используется постоянный Docker volume, закреплённый за контейнером PostgreSQL. Это гарантирует сохранность информации при перезапуске стека или обновлении контейнеров. Каждый сервис имеет собственные переменные окружения для подключения к соответствующей базе данных, что обеспечивает чёткую разграниченность данных и независимость логики отдельных компонентов.
```

**МОЖНО РАСШИРИТЬ ДО:**
```
Для хранения данных используется постоянный Docker volume с именем `postgres_data`, закреплённый за контейнером PostgreSQL. Volume создаётся автоматически при первом развёртывании стека и сохраняется на хосте, что гарантирует сохранность информации при перезапуске стека или обновлении контейнеров. Каждый сервис имеет собственные переменные окружения для подключения к соответствующей базе данных: auth-service использует `AUTH_DATABASE_URL` для подключения к базе `auth_db`, а finance-service использует `FINANCE_DATABASE_URL` для подключения к базе `finance_db`. Такая конфигурация обеспечивает чёткую разграниченность данных и независимость логики отдельных компонентов, при этом оба сервиса используют один и тот же экземпляр PostgreSQL, что упрощает управление и снижает потребление ресурсов.
```

---

## 📝 Итоговая структура раздела 2.2:

1. **Вступление** (строка 133) - общее описание инфраструктуры
2. **Описание сервисов** (строка 134) - состав системы
3. **РАСШИРЕННОЕ: Сетевое взаимодействие** (строка 135) - overlay-сеть, DNS, порты, безопасность
4. **Таблица 4** (после строки 135) - сетевая конфигурация
5. **Описание volumes** (строка 136) - хранение данных
6. **Заключение** (строка 137) - итоги

---

## ✅ Что делать:

1. **Строка 135** - заменить весь абзац на расширенную версию (см. выше)
2. **После строки 135** - добавить Таблицу 4 (сетевая конфигурация)
3. **Опционально** - расширить строку 136 про volumes

Всё готово! Просто замени абзац про сеть и добавь таблицу.

