name: Simple Check

# Запускается при каждом push и pull request
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # Единственная задача - проверить что все работает
  check:
    runs-on: ubuntu-latest
    
    steps:
    # 1. Скачать код
    - name: Get code
      uses: actions/checkout@v4

    # 2. Установить Python
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    # 3. Установить зависимости
    - name: Install packages
      run: |
        pip install fastapi uvicorn sqlalchemy psycopg2-binary python-multipart python-jose[cryptography] passlib[bcrypt] python-dotenv email-validator httpx reportlab pytest pytest-asyncio

    # 4. Проверить что код компилируется
    - name: Check Python code
      run: |
        echo "🔍 Checking if code compiles..."
        python -m py_compile auth-service/app/main.py
        python -m py_compile finance-service/app/main.py
        python -m py_compile report-service/app/main.py
        echo "✅ All Python files are OK"

    # 5. Проверить что сервисы запускаются
    - name: Check services start
      run: |
        echo "🔍 Checking if services can start..."
        # Устанавливаем тестовые переменные окружения
        export AUTH_DATABASE_URL="sqlite:///./test.db"
        export FINANCE_DATABASE_URL="sqlite:///./test.db"
        export FINANCE_SERVICE_URL="http://localhost:8001"
        
        cd auth-service && python -c "from app.main import app; print('✅ Auth service OK')"
        cd ../finance-service && python -c "from app.main import app; print('✅ Finance service OK')"
        cd ../report-service && python -c "from app.main import app; print('✅ Report service OK')"
        echo "✅ All services can start"

    # 6. Запустить функциональные тесты
    - name: Run Functional Tests
      run: |
        echo "🧪 Running functional tests..."
        cd auth-service && python -m pytest tests/test_auth_functional.py -v
        cd ../finance-service && python -m pytest tests/test_finance_functional.py -v
        echo "✅ Functional tests passed!"

    # 7. Запустить тесты базы данных
    - name: Run Database Tests
      run: |
        echo "🗄️ Running database tests..."
        python -m pytest tests/test_database.py -v
        echo "✅ Database tests passed!"

    # 8. Проверить что Docker образы собираются
    - name: Check Docker builds
      run: |
        echo "🔍 Checking if Docker images build..."
        docker build -t test-auth ./auth-service
        docker build -t test-finance ./finance-service
        docker build -t test-report ./report-service
        docker build -t test-frontend ./frontend
        echo "✅ All Docker images build successfully"

    # 9. Проверить что Docker Swarm работает
    - name: Check Docker Swarm
      run: |
        echo "🔍 Checking if Docker Swarm works..."
        # Запускаем Docker Swarm
        docker swarm init --advertise-addr 127.0.0.1 || true
        
        # Собираем образы для теста
        docker build -t fincloud-auth:test ./auth-service
        docker build -t fincloud-finance:test ./finance-service
        docker build -t fincloud-report:test ./report-service
        docker build -t fincloud-frontend:test ./frontend
        
        # Пробуем запустить стек
        docker stack deploy -c docker-compose.swarm-simple.yml fincloud-test
        
        # Ждем немного
        sleep 10
        
        # Проверяем что сервисы запустились
        docker stack services fincloud-test
        
        # Останавливаем тестовый стек
        docker stack rm fincloud-test
        
        echo "✅ Docker Swarm works!"

    # 10. Готово!
    - name: Success
      run: |
        echo "🎉 Everything works! Your code is good to go!"
