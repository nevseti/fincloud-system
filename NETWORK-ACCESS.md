# 🌐 Доступ к FinCloud с других устройств

## Текущая ситуация

Приложение развернуто на VM через Docker Swarm и доступно на следующих портах:
- **Frontend**: `http://localhost:8080`
- **Auth API**: `http://localhost:8000`
- **Finance API**: `http://localhost:8001`
- **Report API**: `http://localhost:8002`

**Проблема**: Сейчас доступно только с самой VM (localhost). Для доступа с других устройств нужно настроить сеть.

---

## 🔧 Решение: Настройка доступа извне

### Вариант 1: Port Forwarding в VirtualBox (Рекомендуется)

**Плюсы**: 
- ✅ Безопасно (порты открыты только на хосте)
- ✅ Не требует изменения настроек VM
- ✅ Работает через NAT

**Минусы**:
- ⚠️ Нужно настроить для каждого порта отдельно

#### Шаги:

1. **Останови VM** (если запущена)

2. **Открой настройки VM в VirtualBox**:
   - Выбери VM → Настройки → Сеть

3. **Настрой Port Forwarding**:
   - Адаптер 1 (NAT) → Расширенные → Port Forwarding
   - Добавь правила:

   | Имя | Протокол | IP хоста | Порт хоста | IP гостя | Порт гостя |
   |-----|----------|----------|------------|----------|------------|
   | Frontend | TCP | 127.0.0.1 | 8080 | | 8080 |
   | Auth API | TCP | 127.0.0.1 | 8000 | | 8000 |
   | Finance API | TCP | 127.0.0.1 | 8001 | | 8001 |
   | Report API | TCP | 127.0.0.1 | 8002 | | 8002 |

4. **Запусти VM**

5. **Подключение**:
   - С других устройств в той же сети: `http://<IP_ТВОЕГО_КОМПЬЮТЕРА>:8080`
   - С самого компьютера: `http://localhost:8080`

---

### Вариант 2: Bridged Adapter (Прямой доступ)

**⚠️ ВАЖНО**: Bridged Adapter дает доступ только в **ЛОКАЛЬНОЙ СЕТИ** (Wi-Fi роутер, домашняя сеть), **НЕ из интернета**!

**Плюсы**:
- ✅ VM получает IP в той же сети, что и хост
- ✅ Прямой доступ без port forwarding в VirtualBox
- ✅ Проще для доступа из локальной сети (телефон, планшет, другие компьютеры в той же Wi-Fi сети)

**Минусы**:
- ⚠️ Менее безопасно (VM видна в локальной сети)
- ⚠️ Нужно настроить firewall на VM
- ⚠️ **НЕ дает доступ из интернета** (нужна отдельная настройка роутера)

#### Шаги:

1. **Останови VM**

2. **Настрой сеть в VirtualBox**:
   - Выбери VM → Настройки → Сеть
   - Адаптер 1: Тип подключения → **Сетевой мост (Bridged Adapter)**
   - Имя: выбери свой сетевой адаптер (Wi-Fi или Ethernet)

3. **Запусти VM**

4. **Узнай IP адрес VM**:
   ```bash
   ip addr show
   # или
   hostname -I
   ```
   Пример вывода: `192.168.1.100`

5. **Настрой firewall на VM** (если нужно):
   ```bash
   sudo ufw allow 8080/tcp
   sudo ufw allow 8000/tcp
   sudo ufw allow 8001/tcp
   sudo ufw allow 8002/tcp
   ```

6. **Подключение**:
   - С других устройств в той же сети: `http://<IP_VM>:8080`
   - Пример: `http://192.168.1.100:8080`

---

## 🔍 Как узнать IP адрес

### На VM (Linux):
```bash
# Способ 1
ip addr show

# Способ 2
hostname -I

# Способ 3
ifconfig
```

### На хосте (Windows):
```powershell
# Узнать IP компьютера в локальной сети
ipconfig
# Ищи "IPv4 Address" в разделе Wi-Fi или Ethernet
```

---

## 📱 Доступ с разных устройств

### С телефона/планшета (в той же Wi-Fi сети):
1. Узнай IP адрес компьютера (где запущена VM)
2. Открой браузер на телефоне
3. Введи: `http://<IP_КОМПЬЮТЕРА>:8080`

### С другого компьютера (в той же сети):
1. Узнай IP адрес компьютера
2. Открой браузер
3. Введи: `http://<IP_КОМПЬЮТЕРА>:8080`

### Из интернета (если нужно):
**⚠️ ВАЖНО**: Это **ОТДЕЛЬНАЯ** настройка, которая работает **ПОВЕРХ** Bridged Adapter или Port Forwarding!

**Что нужно сделать**:

1. **Настрой Bridged Adapter** (как описано выше) или Port Forwarding в VirtualBox

2. **Настрой Port Forwarding на роутере**:
   - Зайди в настройки роутера (обычно `192.168.1.1` или `192.168.0.1`)
   - Найди раздел "Port Forwarding" или "Виртуальные серверы"
   - Добавь правила для портов 8080, 8000, 8001, 8002
   - Укажи IP адрес компьютера (где запущена VM)

3. **Узнай внешний IP адрес**:
   - Зайди на `whatismyip.com` с компьютера
   - Или в настройках роутера найди "WAN IP"

4. **Проверь, что у тебя статический IP**:
   - У большинства провайдеров **динамический IP** (меняется при перезагрузке роутера)
   - Для постоянного доступа нужен **статический IP** (платная услуга) или **Dynamic DNS** (бесплатно, например, через No-IP или DuckDNS)

5. **Открой**: `http://<ВНЕШНИЙ_IP>:8080`

⚠️ **Внимание**: Открытие доступа из интернета требует настройки безопасности:
- Firewall на роутере
- Firewall на компьютере
- **HTTPS** (сейчас у нас HTTP - небезопасно!)
- Аутентификация и авторизация
- Защита от DDoS атак

**Для курсовой работы**: Доступ из интернета обычно **НЕ нужен**. Достаточно доступа из локальной сети.

---

## ✅ Проверка доступности

### С VM:
```bash
curl http://localhost:8080
curl http://localhost:8000/health
```

### С хоста (Windows):
```powershell
# Проверка доступности
Test-NetConnection -ComputerName localhost -Port 8080
Test-NetConnection -ComputerName localhost -Port 8000
```

### С другого устройства:
1. Открой браузер
2. Введи `http://<IP>:8080`
3. Если видишь страницу логина - всё работает! ✅

---

## 🛡️ Безопасность

### Рекомендации:

1. **Firewall на VM**:
   ```bash
   sudo ufw enable
   sudo ufw allow 22/tcp  # SSH
   sudo ufw allow 8080/tcp
   sudo ufw allow 8000/tcp
   sudo ufw allow 8001/tcp
   sudo ufw allow 8002/tcp
   ```

2. **Не открывай порты в интернет** без необходимости

3. **Используй HTTPS** для production (сейчас у нас HTTP)

4. **Ограничь доступ** по IP, если возможно

---

## 🎯 Рекомендация

### Для курсовой работы: **Вариант 1 (Port Forwarding)** ✅

**Почему**:
- ✅ **Проще** - меньше шагов, не нужно менять настройки VM
- ✅ **Безопаснее** - порты открыты только на хосте, не на VM напрямую
- ✅ **Достаточно** - дает доступ из локальной сети (телефон, планшет, другие компьютеры)
- ✅ **Не требует настройки firewall** на VM
- ✅ **Работает одинаково** - результат тот же, что и у варианта 2

**Разница между вариантами 1 и 2**:
- **Вариант 1**: Порт пробрасывается через VirtualBox → доступ через IP компьютера
- **Вариант 2**: VM получает свой IP в сети → доступ через IP VM напрямую
- **Результат**: Оба дают доступ из локальной сети, но вариант 1 проще и безопаснее

### Для production: **Вариант 2 (Bridged Adapter)** + настройка firewall + HTTPS

**Почему**:
- ✅ VM получает свой IP (удобнее для мониторинга)
- ✅ Прямой доступ без промежуточного слоя
- ✅ Лучше для масштабирования

---

## 📝 Пример настройки Port Forwarding

1. VirtualBox → Выбери VM → Настройки → Сеть
2. Адаптер 1 → Расширенные → Port Forwarding
3. Нажми "Добавить правило" (зеленый плюс)
4. Заполни:
   - **Имя**: Frontend
   - **Протокол**: TCP
   - **IP хоста**: 127.0.0.1 (или оставь пустым для всех интерфейсов)
   - **Порт хоста**: 8080
   - **IP гостя**: (оставь пустым)
   - **Порт гостя**: 8080
5. Повтори для портов 8000, 8001, 8002
6. OK → Запусти VM

После этого доступ будет:
- С компьютера: `http://localhost:8080`
- С других устройств: `http://<IP_КОМПЬЮТЕРА>:8080`

