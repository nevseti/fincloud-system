# Локальный запуск FinCloud (без оркестрации)

## 🚀 Быстрый старт:

### 1. Убедись, что Docker запущен

Проверь, что Docker Desktop запущен (или Docker daemon на Linux).

### 2. Запусти систему одной командой:

```bash
docker compose up --build
```

Или если используешь старую версию Docker Compose:

```bash
docker-compose up --build
```

### 3. Дождись запуска всех сервисов

Ты увидишь логи всех контейнеров. Когда все сервисы запустятся, увидишь сообщения типа:
- `auth-service started`
- `finance-service started`
- `report-service started`
- `frontend started`

### 4. Открой в браузере:

**Веб-интерфейс:** http://localhost:8080

**API endpoints:**
- Auth-service: http://localhost:8000
- Finance-service: http://localhost:8001
- Report-service: http://localhost:8002

---

## 📋 Что будет запущено:

1. **PostgreSQL** (порт 5432) - база данных
2. **auth-service** (порт 8000) - сервис аутентификации
3. **finance-service** (порт 8001) - финансовые операции
4. **report-service** (порт 8002) - генерация отчётов
5. **frontend** (порт 8080) - веб-интерфейс

---

## 🛠️ Полезные команды:

### Остановить систему:
```bash
docker compose down
```

### Остановить и удалить volumes (очистить БД):
```bash
docker compose down -v
```

### Запустить в фоновом режиме:
```bash
docker compose up -d --build
```

### Посмотреть логи конкретного сервиса:
```bash
docker compose logs auth-service
docker compose logs finance-service
docker compose logs frontend
```

### Посмотреть статус всех сервисов:
```bash
docker compose ps
```

### Перезапустить конкретный сервис:
```bash
docker compose restart auth-service
```

---

## 🔍 Проверка работоспособности:

### 1. Проверь health endpoints:

```bash
curl http://localhost:8000/health  # auth-service
curl http://localhost:8001/health  # finance-service
curl http://localhost:8002/health  # report-service
```

Должны вернуть: `{"status":"ok"}`

### 2. Открой в браузере:

http://localhost:8080 - должна открыться страница входа

---

## ⚠️ Возможные проблемы:

### Порт уже занят:
Если порт 5432, 8000, 8001, 8002 или 8080 занят, измени порты в `docker-compose.yml` или останови приложение, использующее эти порты.

### Ошибка при сборке:
```bash
# Очисти кэш и пересобери
docker compose build --no-cache
docker compose up
```

### База данных не инициализируется:
```bash
# Удали volumes и пересоздай
docker compose down -v
docker compose up --build
```

---

## 📝 Первый запуск:

При первом запуске:
1. Docker соберёт образы для всех сервисов (может занять несколько минут)
2. PostgreSQL создаст базы данных `auth_db` и `finance_db`
3. Все сервисы запустятся и будут готовы к работе

---

## ✅ Готово!

После запуска система будет доступна по адресу **http://localhost:8080**

Можешь регистрировать пользователей и работать с системой!

