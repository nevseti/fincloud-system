# Раздел 3.7 - Мониторинг и логирование (упрощенный вариант)

## 📍 Где добавить:

**После раздела 3.6** (после Листинга 9), перед "Вывод по третьей главе"

---

## Текст для добавления:

### 3.7. Мониторинг и логирование системы

Для обеспечения надёжной эксплуатации финансовой системы FinCloud реализован мониторинг на основе встроенных возможностей Docker Swarm и стандартных инструментов Linux. Такой подход обеспечивает контроль состояния сервисов без необходимости установки дополнительных компонентов и достаточен для учебного проекта.

**Логирование сервисов** осуществляется через механизм логирования Docker, который автоматически собирает стандартный вывод (stdout/stderr) всех контейнеров. Просмотр логов конкретного сервиса выполняется командой:

```bash
docker service logs fincloud_auth-service
docker service logs fincloud_finance-service
docker service logs fincloud_report-service
docker service logs fincloud_frontend
```

Для отслеживания логов в реальном времени используется флаг `--follow`, что особенно полезно при отладке:

```bash
docker service logs --follow fincloud_auth-service
```

Docker Swarm также предоставляет возможность фильтрации логов по времени и ограничения количества выводимых строк:

```bash
# Последние 100 строк логов
docker service logs --tail 100 fincloud_auth-service

# Логи за последний час
docker service logs --since 1h fincloud_auth-service

# Логи с временными метками
docker service logs --timestamps fincloud_auth-service
```

**Health checks** реализованы на уровне каждого сервиса через эндпоинт `/health`, который возвращает статус работоспособности в формате JSON. Проверка состояния всех сервисов может выполняться через HTTP-запросы:

```bash
curl http://localhost:8000/health  # auth-service
curl http://localhost:8001/health  # finance-service
curl http://localhost:8002/health  # report-service
```

Для автоматизации проверки работоспособности может использоваться простой bash-скрипт:

```bash
#!/bin/bash
services=("auth-service:8000" "finance-service:8001" "report-service:8002")

for service in "${services[@]}"; do
    name=$(echo $service | cut -d: -f1)
    port=$(echo $service | cut -d: -f2)
    if curl -f http://localhost:$port/health > /dev/null 2>&1; then
        echo "✅ $name: OK"
    else
        echo "❌ $name: FAILED"
    fi
done
```

**Мониторинг состояния контейнеров** выполняется через стандартные команды Docker Swarm. Команда `docker service ls` показывает список всех сервисов стека, количество реплик и их состояние:

```bash
docker service ls
```

Для получения детальной информации о конкретном сервисе, включая состояние каждого контейнера, используется команда:

```bash
docker service ps fincloud_auth-service
```

Эта команда отображает состояние контейнеров (Running, Failed, Pending), время запуска, количество перезапусков и идентификаторы контейнеров. Общая информация о стеке доступна через команду:

```bash
docker stack services fincloud
```

**Мониторинг использования ресурсов** может выполняться через команду `docker stats`, которая показывает использование CPU, памяти и сети для всех запущенных контейнеров:

```bash
docker stats --no-stream
```

Для непрерывного мониторинга ресурсов используется команда без флага `--no-stream`, которая обновляет информацию каждую секунду.

**Централизованный просмотр логов** всех сервисов стека может быть организован через комбинацию команд Docker. Например, для просмотра последних логов всех сервисов одновременно:

```bash
docker service logs --tail 50 fincloud_auth-service fincloud_finance-service fincloud_report-service
```

Для production-окружения рекомендуется интеграция специализированных инструментов мониторинга, таких как Prometheus для сбора метрик производительности и Grafana для визуализации данных. Prometheus может собирать метрики через HTTP endpoints сервисов, а Grafana создавать дашборды с графиками использования ресурсов, времени отклика API и количества запросов. Однако для учебного проекта использование встроенных возможностей Docker Swarm и стандартных команд Linux обеспечивает достаточный уровень мониторинга и логирования для контроля состояния системы и диагностики проблем.

В текущей реализации системы FinCloud мониторинг обеспечивается через комбинацию встроенных инструментов Docker Swarm, health checks сервисов и стандартных команд Linux для анализа логов и состояния контейнеров. Такой подход достаточен для демонстрации принципов мониторинга микросервисной архитектуры и может быть расширен при необходимости более детального отслеживания метрик производительности в production-окружении.

---

## 📝 Итоговая структура:

1. **Вступление** - описание подхода к мониторингу
2. **Логирование сервисов** - команды docker service logs
3. **Health checks** - проверка через /health endpoints
4. **Мониторинг состояния** - docker service ls, docker service ps
5. **Мониторинг ресурсов** - docker stats
6. **Централизованный просмотр** - комбинации команд
7. **Заключение** - упоминание Prometheus/Grafana как рекомендация для production

---

## ✅ Что делать:

1. **После Листинга 9** (после строки 331) добавить новый раздел **3.7. Мониторинг и логирование системы**
2. Скопировать текст из этого файла
3. Обновить нумерацию: старый раздел 3.6 остается, новый 3.7 добавляется

Всё готово! Просто вставь текст после раздела 3.6.

