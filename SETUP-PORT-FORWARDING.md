# 🔧 Настройка Port Forwarding в VirtualBox

## Пошаговая инструкция

### Шаг 1: Останови VM (если запущена)

1. Открой VirtualBox
2. Если VM запущена → Выбери VM → **Выключить** (или **Сохранить состояние**)
3. Дождись полной остановки

---

### Шаг 2: Открой настройки VM

1. В списке VM выбери свою VM (например, `fincloud-vm`)
2. Нажми кнопку **"Настройки"** (Settings) или правой кнопкой → **Настройки**

---

### Шаг 3: Перейди в раздел Сеть

1. В левом меню выбери **"Сеть"** (Network)
2. Убедись, что выбран **"Адаптер 1"** (Adapter 1)
3. Проверь, что включено: **"Включить сетевой адаптер"** (Enable Network Adapter)
4. Тип подключения должен быть: **NAT**

---

### Шаг 4: Открой Port Forwarding

1. В разделе "Адаптер 1" нажми кнопку **"Расширенные"** (Advanced)
2. Нажми кнопку **"Port Forwarding"** (Проброс портов)
3. Откроется окно с правилами

---

### Шаг 5: Добавь правила для портов

#### Правило 1: Frontend (порт 8080)

1. Нажми кнопку **"Добавить правило"** (зеленый плюс или "Add")
2. Заполни поля:
   - **Имя** (Name): `Frontend`
   - **Протокол** (Protocol): `TCP`
   - **IP хоста** (Host IP): `127.0.0.1` (или оставь пустым)
   - **Порт хоста** (Host Port): `8080`
   - **IP гостя** (Guest IP): (оставь пустым)
   - **Порт гостя** (Guest Port): `8080`
3. Нажми **OK**

#### Правило 2: Auth API (порт 8000)

1. Нажми **"Добавить правило"**
2. Заполни:
   - **Имя**: `AuthAPI`
   - **Протокол**: `TCP`
   - **IP хоста**: `127.0.0.1` (или пусто)
   - **Порт хоста**: `8000`
   - **IP гостя**: (пусто)
   - **Порт гостя**: `8000`
3. Нажми **OK**

#### Правило 3: Finance API (порт 8001)

1. Нажми **"Добавить правило"**
2. Заполни:
   - **Имя**: `FinanceAPI`
   - **Протокол**: `TCP`
   - **IP хоста**: `127.0.0.1` (или пусто)
   - **Порт хоста**: `8001`
   - **IP гостя**: (пусто)
   - **Порт гостя**: `8001`
3. Нажми **OK**

#### Правило 4: Report API (порт 8002)

1. Нажми **"Добавить правило"**
2. Заполни:
   - **Имя**: `ReportAPI`
   - **Протокол**: `TCP`
   - **IP хоста**: `127.0.0.1` (или пусто)
   - **Порт хоста**: `8002`
   - **IP гостя**: (пусто)
   - **Порт гостя**: `8002`
3. Нажми **OK**

---

### Шаг 6: Сохрани настройки

1. В окне Port Forwarding нажми **OK**
2. В окне настроек VM нажми **OK**

---

### Шаг 7: Запусти VM

1. Выбери VM в списке
2. Нажми **"Запустить"** (Start)
3. Дождись полной загрузки

---

### Шаг 8: Проверь, что все работает

#### На VM (Linux):

```bash
# Проверь, что сервисы запущены
docker service ls

# Проверь доступность
curl http://localhost:8080
curl http://localhost:8000/health
curl http://localhost:8001/health
curl http://localhost:8002/health
```

#### На хосте (Windows):

1. **Узнай IP адрес компьютера**:
   ```powershell
   ipconfig
   ```
   Ищи "IPv4 Address" в разделе Wi-Fi или Ethernet
   Пример: `192.168.1.50`

2. **Проверь доступность**:
   - Открой браузер
   - Введи: `http://localhost:8080` (должна открыться страница логина)
   - Или: `http://192.168.1.50:8080` (с другого устройства в той же сети)

3. **Проверь API**:
   ```powershell
   # Проверка health endpoints
   curl http://localhost:8000/health
   curl http://localhost:8001/health
   curl http://localhost:8002/health
   ```

---

## 📋 Итоговая таблица правил

| Имя | Протокол | IP хоста | Порт хоста | IP гостя | Порт гостя |
|-----|----------|----------|------------|----------|------------|
| Frontend | TCP | 127.0.0.1 | 8080 | | 8080 |
| AuthAPI | TCP | 127.0.0.1 | 8000 | | 8000 |
| FinanceAPI | TCP | 127.0.0.1 | 8001 | | 8001 |
| ReportAPI | TCP | 127.0.0.1 | 8002 | | 8002 |

---

## ✅ Проверка работы

### С компьютера (где запущена VM):
- `http://localhost:8080` → Frontend
- `http://localhost:8000/health` → Auth API
- `http://localhost:8001/health` → Finance API
- `http://localhost:8002/health` → Report API

### С другого устройства в той же сети:
1. Узнай IP адрес компьютера: `ipconfig` (Windows) или `ifconfig` (Linux/Mac)
2. Открой браузер на другом устройстве
3. Введи: `http://<IP_КОМПЬЮТЕРА>:8080`
   - Пример: `http://192.168.1.50:8080`

---

## ❌ Возможные проблемы

### Проблема 1: Порт уже занят
**Ошибка**: "Port 8080 is already in use"

**Решение**:
- Проверь, не запущено ли что-то на этом порту: `netstat -ano | findstr :8080` (Windows)
- Останови процесс, который занимает порт
- Или измени порт хоста на другой (например, 8081)

### Проблема 2: Не могу подключиться с другого устройства
**Решение**:
1. Проверь, что VM запущена
2. Проверь, что сервисы работают: `docker service ls`
3. Проверь firewall на Windows:
   ```powershell
   # Разрешить входящие подключения на порт 8080
   New-NetFirewallRule -DisplayName "FinCloud Frontend" -Direction Inbound -LocalPort 8080 -Protocol TCP -Action Allow
   ```
4. Убедись, что устройства в одной сети (один Wi-Fi роутер)

### Проблема 3: Не вижу кнопку "Port Forwarding"
**Решение**:
- Убедись, что выбран тип подключения **NAT** (не Bridged)
- Нажми "Расширенные" (Advanced) в настройках адаптера

---

## 🎯 Готово!

После настройки Port Forwarding:
- ✅ Доступ с компьютера: `http://localhost:8080`
- ✅ Доступ с других устройств: `http://<IP_КОМПЬЮТЕРА>:8080`
- ✅ Все API endpoints доступны на соответствующих портах

